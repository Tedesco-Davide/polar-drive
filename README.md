# PolarDrive 🚗❄️

Repository unificato per il progetto **PolarDrive**.

# Comandi

FRONTEND:
npm run dev ( avvio classico )
npm i ( re-installa tutti i pacchetti )

BACKEND:
dotnet build ( esegue rebuild soluzione backend )
dotnet run --project PolarDriveInitDB.Cli ( crea nuovo DB da 0 dentro cartella PolarDriveInitDB.Cli )
dotnet run --project PolarDriveInitDBMockData.Cli ( aggiunge dati di mock al nuovo DB creato )
dotnet run --project PolarDrive.WebApi ( avvia WebApi principale )
